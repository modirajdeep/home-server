<% provide(:title, @user.name) %>
<div class="row">
  <div class="page-header">
  <h1><%= render 'shared/user_info' %> 
  <small>
    </small></h1>
</div> 
 <table id="vertical-1">
            <tr>
                <th>Email : </th>
                <td><%= @user.email %></td>
            </tr>
            <tr>
                <th>Upload path : </th>
                <td><%= @user.uploadPath %></td>
            </tr>
        </table>
    <%= link_to "Edit", "#", class: "btn btn-warning" %>
</div>